AUTH_SECRET=secret_key_example
AUTH_SALT=salt_key_example

# Optional: seed default accounts on startup (idempotent — skipped if blank or already exists)
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=change_me_admin_password
DEFAULT_USER_USERNAME=demo_user
DEFAULT_USER_PASSWORD=change_me_user_password